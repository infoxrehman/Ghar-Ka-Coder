<!-- res/values/styles.xml -->
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.GharKaCoder" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentTransitions">true</item>
        <!-- BottomAppBar and BottomNavigationView -->
    </style>

    <!-- Light theme style -->
    <style name="Base.Theme.GharKaCoder" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!-- Define a custom style for active item label -->
    <style name="CustomBottomNavigationActiveLabel" parent="TextAppearance.AppCompat.Caption">
        <item name="android:fontFamily">@font/lato_medium</item>
    </style>

    <!-- Define a custom style for inactive item label -->
    <style name="CustomBottomNavigationInactiveLabel" parent="TextAppearance.AppCompat.Caption">
        <item name="android:fontFamily">@font/lato_medium</item>
    </style>
</resources>